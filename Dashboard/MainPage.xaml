<Page
    x:Class="Dashboard.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dashboard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:customControl="using:Dashboard.CustomControls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <customControl:Widget Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="1" Margin="0 0 0 20" Height="Auto">
            <TextBlock Foreground="Black">Weather Widget</TextBlock>
        </customControl:Widget>

        <customControl:Widget Grid.Column="1" Grid.Row="0" Text="Clock Widget" />

        <customControl:Widget Grid.Column="2" Grid.Row="0" Text="News Widget" />

        <customControl:Widget Grid.Column="0" Grid.Row="1" Text="Notes Widget" />

        <customControl:Widget Grid.Column="1" Grid.Row="1" Text="Todo Widget" />

        <customControl:Widget Grid.Column="2" Grid.Row="1" Text="Quotes Widget" />
    </Grid>
</Page>
