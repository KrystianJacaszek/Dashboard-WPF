<UserControl
    x:Name="userControl"
    x:Class="Dashboard.CustomControls.Widget"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dashboard.CustomControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Grid Padding="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Canvas>
            <controls:DropShadowPanel BlurRadius="4.0" ShadowOpacity="0.70" OffsetX="5.0" OffsetY="5.0" Color="Black">
                <Rectangle Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="1" Fill="White" RadiusX="20" RadiusY="20" Stretch="UniformToFill" Height="80" HorizontalAlignment="Stretch" />
            </controls:DropShadowPanel>

            <Border Padding="20">
                <ContentPresenter Content="{Binding Children, ElementName=userControl}" />
            </Border>
        </Canvas>
    </Grid>
</UserControl>
